// Phase-1 smoke test placeholder. The full app requires Hive/Drift init and a
// device for the WebView, so widget tests are added once those are in place.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('skeleton placeholder', () {
    expect(1 + 1, equals(2));
  });
}
